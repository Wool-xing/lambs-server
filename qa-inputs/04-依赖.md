# 依赖清单（反推: go.mod + docker ps + 部署文档, 2026-08-28）

## 数据库（真机测试矩阵）
- PostgreSQL :5433（主库, lambs）· MySQL :3307 · Redis :6380 · MongoDB :27019 · MSSQL :14333 · Qdrant（App1 按需壳, qdrant://）· SQLite（文件型）
## 外部服务
- TG Bot API（备份上传 + 通知）· SMTP（验证码）· 执行机（tailscale 内网）· nginx（web1）· systemd/journald · Docker（本地测试容器）
## 运行时
- Go 1.x 单二进制 + React SPA；LAMBS_* 环境变量（CONFIG_PATH/DB_MAX_CONNS/MIN_FREE_MB）；deploy.sh 部署链
